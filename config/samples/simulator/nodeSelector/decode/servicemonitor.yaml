apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dev-model-monitor
  namespace: workload-variant-autoscaler-monitoring
  labels:
    app: dev-model-decode
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: dev-model-decode
  namespaceSelector:
    matchNames:
      - llm-d-sim
  endpoints:
    - port: http
      path: /metrics
      interval: 10s
